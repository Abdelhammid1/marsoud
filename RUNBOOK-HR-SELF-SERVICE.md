# 📘 دليل تشغيل: تفعيل حسابات الموظفين (HR Self-Service)

> **لمين الدليل ده؟** OWNER + HR_MANAGER اللي عاوزين يضيفوا موظف جديد ويخلوه قادر يدخل النظام.
> **مدة التشغيل:** ٣ خطوات، حوالي دقيقتين لكل موظف.

---

## 🎯 الفكرة باختصار

لما HR يضيف موظف جديد، النظام يعمل له **حساب User تلقائياً** بحالة `PENDING` (مش قادر يدخل). الـ OWNER بعدها يفتح صفحة التفعيل ويضغط **"تفعيل"** فيوصل للموظف إيميل (أو رابط) يحط فيه كلمة السر ويدخل على بوابته الشخصية.

```
HR يضيف موظف        →   النظام يعمل User (PENDING)
        ↓
OWNER يفتح /hr/accounts/  →   يضغط تفعيل
        ↓
الموظف يستلم رابط  →   يحط كلمة السر  →  يدخل بوابته /my/
```

---

## 📋 السيناريو الأساسي — من الصفر للدخول

### الخطوة 1️⃣ — HR_MANAGER يضيف الموظف

**الرابط:** `https://marsoud.manasety.ai/payroll/employees/new`

في الفورم، الحقول الأساسية:

| الحقل | إلزامي؟ | ملحوظة |
|---|---|---|
| الاسم | ✅ | كاسم الموظف الكامل |
| **البريد الإلكتروني** | ✅ **إلزامي** | الإيميل ده هو اللي هيستخدمه عند الدخول |
| الهاتف | ❌ | اختياري |
| المسمى الوظيفي | ❌ | اختياري |
| الراتب الأساسي | ✅ | لتوليد كشوفات الرواتب |
| تاريخ الانضمام | ✅ | افتراضي = النهارده |
| القسم | ❌ | اختياري |
| نوع العقد | ✅ | FULL_TIME / PART_TIME / CONTRACT |

اضغط **"حفظ"**.

#### اللي بيحصل في الخلفية:
- يُنشأ User تلقائياً بالـ email ده
- `status = PENDING` (في انتظار التفعيل)
- `is_active = False` (مش قادر يدخل بعد)
- `employee_id` مربوط بالموظف الجديد
- الـ role الافتراضي = `employee` (بوابة شخصية فقط)
- يظهر رسالة: *"تم إنشاء حساب الموظف بحالة PENDING — يحتاج للتفعيل من المالك"*

> ⚠ **مهم:** لو الـ email موجود قبل كده لـ User تاني (مثلاً OWNER اللي بيدخل بنفس البريد)، النظام **مايعملش User جديد**، بس بيربط الـ Employee الجديد بالـ User الموجود.

---

### الخطوة 2️⃣ — OWNER يفعّل الحساب

**الرابط:** `https://marsoud.manasety.ai/hr/accounts/`
**أو من السايدبار:** العمليات → الموارد البشرية → **تفعيل حسابات الموظفين** 🔑

الصفحة بتعرض كل المستخدمين بحالة PENDING في جدول:

| الاسم | البريد | تاريخ الإنشاء | الإجراء |
|---|---|---|---|
| محمد عبدالله | mohamed@... | 2026-06-13 | [✅ تفعيل وإرسال إيميل] [تعطيل] |

اضغط **"✅ تفعيل وإرسال إيميل"**.

#### اللي بيحصل:
1. `status` يتغير لـ `ACTIVE`، `is_active = True` ✓
2. النظام يولّد Invitation token صلاحيته 14 يوم
3. النظام يحاول يبعت إيميل للموظف فيه رابط تعيين كلمة السر
4. **لو الإيميل اتبعت بنجاح:** يظهر "تم تفعيل {الاسم} وإرسال إيميل تعيين كلمة السر" 🟢
5. **لو SMTP مش متفعّل:** يظهر "تم تفعيل {الاسم}. شارك معه الرابط يدوياً: `https://marsoud.manasety.ai/invitations/accept/...`" 🟡 — تنسخ الرابط وتبعته يدوياً (واتساب / SMS / إلخ)

---

### الخطوة 3️⃣ — الموظف يحط كلمة السر

الموظف بيفتح الرابط من الإيميل (أو اللي بعته OWNER يدوياً) ويظهر له:

```
─────────────────────────
       دعوة للانضمام
   شركة الأمل التجارية
mohamed@example.com — صلاحية حتى 2026-06-27
─────────────────────────
  تفعيل حسابك الجديد — اختر
   كلمة مرور (6 أحرف على الأقل)

   ┌─────────────────────┐
   │  كلمة المرور        │
   └─────────────────────┘

   [قبول الدعوة والدخول]
─────────────────────────
```

يحط كلمة سر → يضغط زرار → يدخل النظام مباشرةً → ياخده على `/my/`.

---

### الخطوة 4️⃣ — الموظف يستخدم بوابته

**الرابط:** `https://marsoud.manasety.ai/my/`

الموظف بيشوف صفحة واحدة فيها **٤ أقسام**:

#### 🧑 ١. ملفه الشخصي
الاسم، القسم، المسمى الوظيفي، تاريخ الانضمام، نوع العقد، رقم الموظف.

#### 💰 ٢. كشوفاته المالية
جدول بكل كشوفات الرواتب (من تاريخ الانضمام):
- الفترة (سنة-شهر)
- الأساسي / البدلات / المكافآت / الإضافي / الخصومات / **الصافي**
- زرار **⬇ PDF** لتحميل قسيمة كل شهر

#### 📜 ٣. تاريخه الوظيفي
Timeline بترتيب زمني:
- 🎉 الانضمام للشركة (يوم كذا)
- 🏢 نقل قسم: من "مبيعات" لـ "مبيعات تنفيذية" (تاريخ + من اللي عمله)
- 💼 تغيير المسمى الوظيفي: من "Junior" لـ "Senior"
- 💰 تعديل الراتب: من 5000 لـ 7500

#### 🌴 ٤. إجازاته
- رصيد كل نوع إجازة (سنوية، عارضة، إلخ)
- زرار **"+ طلب إجازة جديدة"** يفتح فورم → يختار النوع + التاريخ → يبعت
- جدول الطلبات السابقة وحالة كل واحد (قيد الاعتماد / معتمدة / مرفوضة)

**أي طلب إجازة جديد** يوصل تلقائياً للـ HR_MANAGER في `/hr/leave_requests` للاعتماد.

> 🚫 **مهم:** الموظف **مش قادر يدخل أي صفحة تانية في النظام**. لو حاول يفتح `/invoices/` أو `/inventory/` بياخد 403. الحد الأقصى ليه هو بوابته فقط.

---

## ⚙ سيناريو متقدّم — موظف بصلاحيات إضافية

أحياناً موظف يبقى ليه **role إضافي** زي HR_MANAGER أو ACCOUNTANT، عشان يقدر يدير أنشطة معينة بجانب بوابته الشخصية.

#### إزاي تديله الـ role الإضافي:

##### الطريقة 1️⃣ — من إدارة الأدوار (الأسهل)
**الرابط:** `/settings/roles/?role_id=<role_id>`

1. افتح الـ role اللي عاوز تربط الموظف بيه (مثلاً HR_MANAGER)
2. اضغط تبويب **"الأدمن"**
3. في dropdown "اختر أدمن لإضافته…" → اختار الموظف → اضغط **"ربط"**

النتيجة: الـ role بتاع الموظف على الشركة يتحول من `employee` للـ role الجديد.

##### الطريقة 2️⃣ — من صفحة المستخدمين
**الرابط:** `/users/`

1. الصف بتاع الموظف → في عمود "الدور" → افتح القائمة المنسدلة
2. اختار الـ role الجديد (HR_MANAGER / ACCOUNTANT / إلخ)
3. النظام يحفظ تلقائياً

#### اللي بيظهر للموظف بعد الترقية:
- بوابته الشخصية `/my/` **ما تخفيش** (ممكن يفتحها متى عاوز)
- بالإضافة لكدا بيشوف صفحات الـ role الجديد في السايدبار:
  - **HR_MANAGER:** الموارد البشرية + الأقسام + طلبات الإجازات + الرواتب (عرض)
  - **ACCOUNTANT:** المحاسبة كاملة + المخزون + POS + التقارير المالية

---

## 🔧 حالات شائعة وحلولها

### ❓ ضغطت "تفعيل" بس الإيميل ما وصلش للموظف

السبب الأرجح: **SMTP مش متفعّل في إعدادات الخادم**.

**الحل:** بعد ما تضغط "تفعيل"، انتبه للرسالة الصفراء في أعلى الصفحة. هتلاقي فيها الرابط زي:

```
تم تفعيل محمد. شارك معه الرابط يدوياً:
https://marsoud.manasety.ai/invitations/accept/eyJlbWFpbCI6...
```

انسخ الرابط ده وابعته للموظف على واتساب أو SMS.

---

### ❓ الموظف بيقول له "حسابك غير مفعّل أو موقوف"

يبقى لسه `status = PENDING` أو `is_active = False`.

**الحل:** OWNER يدخل على `/hr/accounts/` → يضغط "تفعيل" تاني.

---

### ❓ الرابط منتهي الصلاحية

الـ Invitation صلاحيته **14 يوم** من وقت التفعيل.

**الحل:** OWNER يضغط "تفعيل" تاني من `/hr/accounts/` (الزرار بيولّد token جديد لو الموظف لسه ما حطش كلمة سر).

---

### ❓ الموظف عاوز يغير كلمة السر بعد كده

حالياً مش متاح من واجهة الموظف. كحل مؤقت: OWNER يدخل على `/hr/accounts/` يفعّل ليه تاني → يولّد token جديد → الموظف يفتح الرابط ويحط كلمة سر جديدة.

---

### ❓ موظف مفصول / استقال

**الحل:** OWNER يدخل `/hr/accounts/` → يضغط **"تعطيل"** بدل التفعيل. الحساب يبقى DISABLED (مش قادر يدخل) لكن كل بياناته وتاريخه المالي محفوظين للأرشيف.

---

## ✅ معايير القبول (checklist للاختبار)

- [ ] إضافة موظف بـ email → User ينشأ تلقائياً بـ PENDING
- [ ] PENDING ما يقدرش يدخل أي صفحة في النظام
- [ ] OWNER يضغط "تفعيل" → الموظف يستلم رابط (أو يظهر للـ OWNER)
- [ ] أي تعديل على القسم / الراتب / المسمى → يُسجَّل في EmployeeHistory تلقائياً
- [ ] الموظف يشوف كشوفاته ويحمّل PDF لأي قسيمة
- [ ] طلب الإجازة من البوابة يصل للـ HR_MANAGER للاعتماد

---

## 📂 الـ Routes / Endpoints المرجعية

| الـ Route | الوظيفة | اللي يقدر يدخل |
|---|---|---|
| `POST /payroll/employees/new` | إضافة موظف جديد | HR_MANAGER + OWNER + ADMIN |
| `GET /hr/accounts/` | قائمة الحسابات PENDING | OWNER (`users.manage`) |
| `POST /hr/accounts/<id>/activate` | تفعيل حساب | OWNER (`users.manage`) |
| `POST /hr/accounts/<id>/disable` | تعطيل حساب | OWNER (`users.manage`) |
| `GET /invitations/accept/<token>` | الموظف يحط كلمة السر | عام (بدون login) |
| `GET /my/` | بوابة الموظف | EMPLOYEE فقط |
| `GET /my/payslip/<id>.pdf` | تحميل قسيمة راتب | EMPLOYEE نفسه فقط |
| `POST /my/leave/new` | طلب إجازة جديدة | EMPLOYEE فقط |

---

## 🛠 ملاحظات للمطور

- جدول `users` فيه عمودين: `status` (ACTIVE/PENDING/DISABLED) + `is_active` (Boolean). الاتنين بيتزامنوا في `services/hr_self_service.py`.
- صفحة `/auth/login` بتمنع `is_active=False` (يشمل PENDING + DISABLED).
- صفحة `/my/` محمية بالـ `EMPLOYEE_ALLOWED_ENDPOINTS` في `app/__init__.py` — أي route تاني للموظف يرجع 403.
- جدول `employee_history` بيتسجّل فيه أوتوماتيك كل تعديل على القسم/المسمى/الراتب من خلال `services/payroll.update_employee()`.
- جدول `invitations` فيه الـ tokens، صلاحيتهم 14 يوم.
