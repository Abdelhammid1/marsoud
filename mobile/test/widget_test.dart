// MARSOUD-MOBILE-FLUTTER — placeholder widget test.
//
// The default counter test that `flutter create` scaffolds no longer
// matches our app; kill it here rather than deleting the file so
// `flutter test` reports "0 tests, 0 failures" instead of a red build.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
