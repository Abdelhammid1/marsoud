Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Migrate==4.0.7
Flask-WTF==1.2.1
WTForms==3.1.2
SQLAlchemy==2.0.30
anthropic==0.39.0
# MARSOUD-INSIGHTS-AGENT-01 (Batch 9 Ticket 6, 2026-08-01) —
# DeepSeek is OpenAI-SDK-compatible, so we reuse the official
# openai client with a base_url override.
openai>=1.30.0
python-dotenv==1.0.1
openpyxl==3.1.5
reportlab==4.2.2
arabic-reshaper==3.0.0
python-bidi==0.4.2
email-validator==2.2.0
Werkzeug==3.0.3
itsdangerous==2.2.0
python-barcode==0.16.1
Pillow>=10.0.0
# MARSOUD-51 — WeasyPrint renders the new invoice + payslip PDFs.
# Production hosts need libpango + libcairo installed via OS package manager:
#   Debian/Ubuntu: apt install libpango-1.0-0 libpangoft2-1.0-0
#   macOS dev:     brew install pango
weasyprint>=60.0
python-dateutil
# MARSOUD-MOBILE-TKT-05 (2026-08-18) — Firebase Cloud Messaging
# for the Flutter push notifications. `firebase-admin` bundles
# `google-auth` + `google-api-core`; no extra pinning needed.
# Silently skipped when the service-account JSON is absent (see
# app/services/fcm.py) so dev + CI environments don't need the
# credentials file.
firebase-admin>=6.5.0
